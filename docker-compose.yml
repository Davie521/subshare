services:
  subshare:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - subshare-data:/app/data
    environment:
      - SESSION_SECRET=${SESSION_SECRET:-change-me-to-a-random-32-char-string}
      - DATABASE_URL=/app/data/subshare.db
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  subshare-data:
